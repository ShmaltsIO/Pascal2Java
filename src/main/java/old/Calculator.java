package old;

import java.io.StringReader;

public class Calculator {
    /*public static int calc(String expr) throws Exception {
        parser p = new parser(new CalcLexer(new StringReader(expr)));
        return (Integer) p.parse().value;
    }

    public static String generateCodeForJasmin(String expression) throws Exception {
        parser pars = new parser(new CalcLexer(new StringReader(expression)));

        return (String) pars.parse().value;
    }*/
}